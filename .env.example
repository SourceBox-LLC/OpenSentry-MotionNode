# OpenSentry Camera Node Configuration
# Copy to .env and edit, or just run ./setup.sh

# Camera name (shown in dashboard)
CAMERA_NAME=My Camera

# Camera device (run: ls /dev/video* to find yours)
CAMERA_DEVICE=/dev/video0

# Security secret (must match Command Center!)
OPENSENTRY_SECRET=
