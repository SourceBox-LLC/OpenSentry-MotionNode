# OpenSentry Node Configuration
# Copy this file to .env and customize for your setup

# Unique camera identifier (must be unique per node)
CAMERA_ID=camera1

# Display name shown in Command Center
CAMERA_NAME=Living Room Camera

# MQTT broker address (for connecting to external broker)
MQTT_SERVER=tcp://127.0.0.1:1883

# Camera device path
CAMERA_DEVICE=/dev/video0

# ============================================================================
# SINGLE SECRET MODE (Recommended)
# ============================================================================
# Set this ONE secret and all MQTT/RTSP credentials will be derived from it
# Must be the same on all Nodes and Command Center
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
OPENSENTRY_SECRET=

# ============================================================================
# LEGACY MODE (Individual credentials)
# ============================================================================
# Only used if OPENSENTRY_SECRET is not set
# MQTT_USERNAME=opensentry
# MQTT_PASSWORD=opensentry
# RTSP_USERNAME=opensentry
# RTSP_PASSWORD=opensentry

# Port mappings (change if running multiple nodes on same host)
RTSP_PORT=8554
RTMP_PORT=1935
HLS_PORT=8888
WEBRTC_PORT=8889
