# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║              OPENSENTRY CAMERA NODE - CONFIGURATION FILE                   ║
# ╚═══════════════════════════════════════════════════════════════════════════╝
#
# HOW TO USE THIS FILE:
# 1. Copy this file and rename it to ".env" (remove the .example part)
#    Command: cp .env.example .env
# 2. Edit the values below to match your setup
# 3. Start the camera: docker compose up --build
#
# IMPORTANT: The .env file contains secrets - never share it or commit to git!


# ═══════════════════════════════════════════════════════════════════════════
# STEP 1: NAME YOUR CAMERA
# ═══════════════════════════════════════════════════════════════════════════
# This is the friendly name that will appear in your Command Center dashboard.
# Example: "Front Door", "Living Room", "Backyard", etc.

CAMERA_NAME=My Camera


# ═══════════════════════════════════════════════════════════════════════════
# STEP 2: SET YOUR SECURITY SECRET
# ═══════════════════════════════════════════════════════════════════════════
# This secret secures communication between this camera and the Command Center.
#
# ⚠️  CRITICAL: This MUST be the SAME value on:
#     - The Command Center
#     - Every Camera Node (including this one!)
#
# To generate a random secret, run this command:
#     python -c "import secrets; print(secrets.token_hex(32))"
#
# Then paste the result below (it will look like a long string of letters/numbers)

OPENSENTRY_SECRET=


# ═══════════════════════════════════════════════════════════════════════════
# STEP 3: CHECK YOUR CAMERA DEVICE (usually don't need to change)
# ═══════════════════════════════════════════════════════════════════════════
# This is the path to your USB webcam.
# 
# To find your camera, run: ls /dev/video*
# Most USB webcams are /dev/video0

CAMERA_DEVICE=/dev/video0


# ═══════════════════════════════════════════════════════════════════════════
# OPTIONAL: CAMERA ID
# ═══════════════════════════════════════════════════════════════════════════
# A unique identifier for this camera. If you leave this empty, it will
# automatically use the device's hostname (recommended for most users).
#
# Only set this if you're running multiple cameras on the same device.

# CAMERA_ID=front-door


# ═══════════════════════════════════════════════════════════════════════════
# ADVANCED SETTINGS (only change if you know what you're doing)
# ═══════════════════════════════════════════════════════════════════════════

# MQTT broker address (uses built-in broker by default)
MQTT_SERVER=tcp://127.0.0.1:1883

# Port mappings (change if running multiple nodes on same host)
RTSP_PORT=8554
RTMP_PORT=1935
HLS_PORT=8888
WEBRTC_PORT=8889

# Legacy mode credentials (only used if OPENSENTRY_SECRET is empty)
# MQTT_USERNAME=opensentry
# MQTT_PASSWORD=opensentry
# RTSP_USERNAME=opensentry
# RTSP_PASSWORD=opensentry
