# OpenSentry Node Configuration
# Copy this file to .env and customize for your setup

# Unique camera identifier (must be unique per node)
CAMERA_ID=camera1

# Display name shown in Command Center
CAMERA_NAME=Living Room Camera

# MQTT broker address
# Use host.docker.internal for local MQTT broker on host machine
# Or specify IP address for remote broker
MQTT_SERVER=tcp://host.docker.internal:1883

# Camera device path
CAMERA_DEVICE=/dev/video0

# Port mappings (change if running multiple nodes on same host)
RTSP_PORT=8554
RTMP_PORT=1935
HLS_PORT=8888
WEBRTC_PORT=8889
